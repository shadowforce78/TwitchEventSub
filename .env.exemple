TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
# TWITCH_REDIRECT_URI=http://localhost:3001/auth/twitch/callback
# Optional: set this when testing with ngrok; it overrides TWITCH_REDIRECT_URI
# Example: https://<your-subdomain>.ngrok.app/auth/twitch/callback
NGROK_REDIRECT_URI=https://twitch.saumondeluxe.com/auth/twitch/callback
SESSION_SECRET=
# Optional: secret used for EventSub signature validation (defaults to SESSION_SECRET)
EVENTSUB_SECRET=
PORT=6969
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306
# Enable SSL for hosting providers that require it (true/false)
DB_SSL=false
# Whether to reject unauthorized certs when SSL is true (true/false)
DB_SSL_REJECT_UNAUTHORIZED=true